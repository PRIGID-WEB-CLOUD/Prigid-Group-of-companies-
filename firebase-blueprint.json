{
  "entities": {
    "Mail": {
      "title": "Mail",
      "description": "Outbound email queue document for transactional email delivery.",
      "type": "object",
      "properties": {
        "to": {
          "type": "string",
          "description": "Recipient email address",
          "maxLength": 320
        },
        "storeId": {
          "type": "string",
          "description": "Store identifier associated with email",
          "maxLength": 64
        },
        "message": {
          "type": "object",
          "description": "Email message payload containing subject, text, and optional html."
        },
        "delivery": {
          "type": "object",
          "description": "Delivery tracking metadata including state and start timestamp."
        }
      },
      "required": ["to", "message", "delivery"]
    }
  },
  "firestore": {
    "/mail/{mailId}": {
      "schema": "Mail",
      "description": "Collection storing transactional email documents to be dispatched by email triggers."
    }
  }
}
